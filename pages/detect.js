import axios from 'axios';

function Detect(props) {
  // axios.get('http://localhost:3000/api/hello').then((doc) => {
  //   console.log(doc);
  // });

  return <div>hey</div>;
}

export default Detect;
